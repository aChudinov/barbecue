export default ({ colors }) => ({
  container: {
    backgroundColor: '#826B60',
    minHeight: '100%',
  },

  heading: {
    color: '#E4DDDB',
    textAlign: 'center',
    textTransform: 'uppercase',
  },

  subheading: {
    textAlign: 'center',
    textTransform: 'uppercase',
  },
});
